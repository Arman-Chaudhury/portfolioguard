import React from "react";
import ReactDOM from "react-dom/client";
import PortfolioGuard from "./PortfolioGuard";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PortfolioGuard />
  </React.StrictMode>
);